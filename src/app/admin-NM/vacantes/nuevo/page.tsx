import AdminJobNew from "@/views/admin/AdminJobNew";

export const metadata = {
  title: "Nueva Vacante | Nasua Admin",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <AdminJobNew />;
}
